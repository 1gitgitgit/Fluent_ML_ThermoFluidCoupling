# Notes
## 1. Re
通过雷诺数计算，增强物理可解释性，同时在任何机器学习等开始之前先输出判断，是否为层流，相对于纯口入速度更准确判断流态
## 2. 三模型优劣对比
结合你的结果数据：
RFLGBMLP精度0.99990.9999修复后待定训练速度慢快中可解释性有特征重要性有特征重要性黑盒对数据量敏感不敏感不敏感敏感对量纲敏感不敏感不敏感敏感，必须标准化
"RF和LGB都是树模型，不需要标准化，对特征量纲不敏感，在我这个数据集上精度相当。LGB训练更快，适合工况多的场景。MLP理论上能拟合更复杂的非线性关系，但对预处理要求更严，数据量小时容易不如树模型。"
## 3. 调参思路
RF 的关键参数：

n_estimators：树的数量，越多越稳定但收益递减，200够用
max_depth：控制过拟合，你用了15，可以试试不限制（None）对比一下
调参优先级：先确定 max_depth，再增加 n_estimators

LGB 的关键参数：

num_leaves：叶子数控制复杂度，你用63，是2^6-1，经验上不超过样本数的千分之一
learning_rate + n_estimators 是一对：学习率越小需要越多树，你用0.05+500是合理组合
调参优先级：先定 num_leaves，再用早停自动定树的数量

MLP 的关键参数：

网络结构：你用256→256→128→64，逐层缩小是合理的，宽进窄出
learning_rate_init：1e-3是Adam默认值，如果loss震荡可以降到1e-4
early_stopping：你开了，patience=30，防止过拟合的关键
调参优先级：先确定网络深度，再调学习率


## 4. 代理模型误差怎么评估
你已有三个指标，讲清楚每个的含义和局限：
RMSE：和目标量同量纲，直觉上知道误差大小，但不同物理量之间没法比较（速度的RMSE和压力的RMSE数量级差很多）
NRMSE：除以了量程，变成百分比，跨物理量可以比较，也是你项目里最重要的指标，低于5%算工程可用，低于2%算优秀
R²：反映模型解释了多少方差，接近1说明预测趋势对，但R²高不代表误差小，两个指标要结合看
面试怎么说：

"我用NRMSE作为主要评估指标，因为它消除了量纲影响，三个物理量可以横向比较。RF和LGB都在0.3%以内，工程上完全可用。"
## 5.为什么选这几个工况
选了：
速度：0.01~0.5 m/s，15个点
温度：280~310K，5个点

背后的逻辑叫实验设计（DOE），面试时要能说清楚：
范围怎么定：速度范围覆盖你关心的工程场景，温度范围对应实际散热工况，边界要够宽让模型有足够的外推能力
点数怎么定：速度对流场影响更非线性，所以取15个点；温度影响相对线性，5个点够了。总工况75组是计算资源和精度的权衡
分布怎么选：你用的是均匀分布，更严格的做法是拉丁超立方采样（LHS），能用更少的点覆盖更大的参数空间，这个可以作为改进点提
## 6. 闭环验证的意义
闭环验证就是：从Pareto前沿取2-3个代表点，回Fluent跑真实仿真，对比代理模型预测值和CFD结果的误差。
意义有三层：
第一层，验证代理模型可信度：训练集和测试集都是已知工况插值，Pareto点可能在训练数据边界附近，闭环验证才能证明模型在优化推荐的点上是否还准
第二层，验证优化结果有效性：代理模型预测说这个点散热好、泵功低，CFD要验证这个结论是否成立，否则优化只是在代理模型上的数学游戏
第三层，体现工程严谨性：没有闭环验证的优化结果在工程上不可信，这也是这个项目区别于纯算法项目的地方，面试时说"我做了闭环验证，CFD误差在X%以内"比只说"跑了优化"有说服力得多。
